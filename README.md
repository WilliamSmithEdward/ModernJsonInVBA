# ModernJsonInVBA
Turn JSON text into clean Excel tables with one VBA module. Pick the part of the JSON you want, and it creates or updates a table for you. Keeps columns in a stable order and prevents accidental layout changes. No add-ins or extra libraries required. Just paste into Excel and run.
